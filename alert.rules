groups:
- name: selinux example
  rules:
  - alert: selinux disabled
    expr: node_selinux_current_mode == 0
    for: 1s
    labels:
      severity: page
    annotations:
      summary: "selinx disabled"
